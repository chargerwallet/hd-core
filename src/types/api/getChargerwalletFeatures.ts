import type { CommonParams, Response } from '../params';
import type { Chargerwalletatures } from '../device';

export declare function getChargerwalletatures(
  connectId?: string,
  params?: CommonParams
): Response<Chargerwalletatures>;
